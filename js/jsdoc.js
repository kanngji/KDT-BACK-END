/**
 *
 * @param {string} name 사람 이름을 넣으시면 됩니다
 * @param {number} age 나이를 숫자로 넣으세요!
 * @returns name 과 age가 들어간 문자열을 반홥합니다.
 */
function hello(name, age) {
  return `내 이름은 ${name} 이고 나이는 ${age} 입니다`;
}

const str = hello();
